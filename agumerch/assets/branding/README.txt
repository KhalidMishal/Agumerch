Add the official AGU logo image as `agu_logo.png` in this folder.
Recommended specs:
- PNG, square canvas (1024x1024 or larger)
- Transparent background
- Centered logo with safe padding

After placing the file, run `flutter pub get` followed by `flutter pub run flutter_launcher_icons`. This will generate platform-specific launcher icons.
